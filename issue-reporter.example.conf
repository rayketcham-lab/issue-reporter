# Bash config for issue-reporter.sh
# Copy to issue-reporter.conf (same directory as the script)

PROJECT_NAME="My Awesome App"
REPO_DIR="."

# Free local AI (install: brew install ollama && ollama pull llama3)
# OLLAMA_MODEL="llama3"
# OLLAMA_URL="http://localhost:11434"
